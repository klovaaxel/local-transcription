org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin incremental compilation cannot close its .tab caches on this machine -
# every plugin module fails with "Could not close incremental caches" mid-build.
# That is file locking from endpoint protection, not a Gradle bug. A release
# build compiles everything from scratch anyway, so incremental buys nothing.
kotlin.incremental=false
